# 課程計畫 AI 審查工具 — 開發進度與未來規劃

> 最後更新：2026-04-10　｜　作者：阿凱老師　｜　目前版本：**v1.9**

---

## 一、已完成功能清單（v1.0 → v1.9）

### ✅ 核心架構
| 項目 | 說明 | 完成版本 |
|------|------|------|
| 單一 HTML 檔案 | 純前端、無後端、無框架依賴 | v1.0 |
| GitHub Pages 部署 | 線上可直接使用，免安裝 | v1.0 |
| 響應式設計（RWD） | 手機 / 平板 / 桌機自適應（480px / 768px 斷點） | v1.6 |
| localStorage 儲存設定 | API 金鑰、模型選擇本地儲存，不上傳伺服器 | v1.0 |
| CSS 設計系統（Custom Properties） | --step1 藍 / --step2 紫 / --step3 琥珀 三步驟配色 | v1.6 |
| Google Fonts 整合 | Nunito + Noto Sans TC 雙字型搭配 | v1.6 |
| PWA manifest.json | 名稱 / 圖示 / 色彩 / 截圖，支援 iOS / Android 桌面安裝 | v1.8 |
| Service Worker（sw.js） | 靜態資源離線快取，讓工具在無網路時仍可開啟 | v1.8 |

### ✅ PDF 擷取
| 項目 | 說明 | 完成版本 |
|------|------|------|
| 拖拉 / 點擊上傳 PDF | 支援兩種上傳方式 | v1.0 |
| PDF.js 懶載入 | 首次使用才載入 CDN，加速初始頁面 | v1.0 |
| PDF.js 升級至 v4.4.168 | ES Module 動態 import，支援更多 PDF 格式 | v1.5 |
| 全頁文字擷取 | 逐頁累加文字，顯示總頁數與字數 | v1.0 |
| 文字預覽（前 500 字） | 讓使用者確認擷取品質 | v1.0 |
| 掃描版 PDF 警告 | 擷取字數 < 50 時提示無法識別 | v1.0 |
| 重新上傳按鈕 | 修正後可快速清除重來 | v1.2 |
| PDF 字數過長警告 | 超過 50,000 字時顯示橘色警告，提示可能超出 Token 上限 | v1.5 |

### ✅ 審查項次資料
| 項目 | 說明 | 完成版本 |
|------|------|------|
| 40+ 項審查標準內嵌 | 涵蓋 0-1 至 7-12 所有項次 | v1.0 |
| 分類下拉選單 | 8 大分類（0–7）自動產生 | v1.0 |
| 項次下拉選單 | 依分類動態更新 | v1.0 |
| 可編輯審查提示詞 | Textarea 直接修改條件（如課程門數） | v1.2 |
| 還原預設提示詞按鈕 | 一鍵恢復原始 Prompt | v1.2 |
| 特殊班級警告標語 | special:true 項次自動顯示需人工審查提醒 | v1.2 |
| 項次白話說明（ITEM_DESC） | 每個項次旁顯示「這個項次在審查什麼」的白話摘要 | v1.5 |
| 項次即時搜尋 | 搜尋框即時篩選 40+ 項次，支援 id / 名稱搜尋，單字符觸發 | v1.8 |

### ✅ API 整合
| 項目 | 說明 | 完成版本 |
|------|------|------|
| Google Gemini API | gemini-2.5-flash / 2.5-pro / 2.0-flash | v1.0 |
| OpenAI API | gpt-4o-mini / gpt-4o / gpt-4.1-mini | v1.0 |
| 模型切換下拉 | 依供應商動態更新模型清單 | v1.0 |
| 免費申請 API 金鑰連結 | 依供應商自動切換連結目標 | v1.0 |
| 測試 API 金鑰按鈕 | 發送最小請求即時驗證金鑰有效性 | v1.4 |
| 智慧錯誤提示 | 依錯誤類型（無效 / 額度耗盡 / CORS）給出中文說明 | v1.4 |
| 金鑰顯示 / 隱藏切換（👁） | 密碼欄位右側切換按鈕，確認輸入正確 | v1.5 |
| CORS 限制警告（OpenAI） | 自動提示改用 Gemini 或複製提示詞 | v1.0 |

### ✅ 審查流程（單項）
| 項目 | 說明 | 完成版本 |
|------|------|------|
| 三步驟引導流程 | Step 1 上傳 → Step 2 選擇 → Step 3 審查 | v1.0 |
| 三步驟彩色卡片設計 | 藍 / 紫 / 琥珀 三種顏色對應各步驟 | v1.6 |
| 送出 AI 審查 | 組裝 Prompt，呼叫 API，顯示結果 | v1.0 |
| Markdown 結果美化渲染 | 標題、粗體、列表自動轉換，區塊間距優化 | v1.5 |
| 審查結果區塊色彩標示 | 「符合」→ 綠色卡片；「待修正」→ 紅色卡片 | v1.5 |
| 上次審查記錄橫幅 | 首頁顯示上次審查的項次 + 時間，localStorage 持久化 | v1.5 |
| 複製提示詞（Markdown） | 無 API 金鑰時可貼到 ChatGPT / Gemini 使用 | v1.0 |
| 複製純文字 | 去除 `**`、`##` 符號後複製，可直接貼入 Word | v1.8 |
| 修正後重新上傳審查 | 一鍵清除並回到上傳步驟 | v1.2 |
| 列印審查結果 | @media print A4 版面，結果區塊保色 | v1.8 |
| 快捷鍵（Ctrl+Enter）| 送出審查，Cmd+Enter 也支援 | v1.8 |
| AI 限制警語橫幅 | 提醒審查僅供參考，以人工審定為準 | v1.2 |
| Loading 動畫 | 旋轉圈圈 + 等待時間提示 | v1.0 |
| Toast 通知 | 操作回饋（儲存成功、複製成功等） | v1.0 |

### ✅ 批次審查與報告（v1.8 新增 ⭐）
| 項目 | 說明 | 完成版本 |
|------|------|------|
| 單項 / 批次模式切換 Tab | Step 2 頂部切換，介面完全獨立 | v1.8 |
| 批次項目 Chip 選擇器 | 彩色圓角 Chip，全選 / 分類全選 / 單項點選 | v1.8 |
| 批次進度條 | 逐項顯示目前進度、項目名稱、剩餘數量 | v1.8 |
| 批次暫停 / 繼續 | 中途可暫停，不喪失已完成結果 | v1.8 |
| 批次完成摘要 | ✅ 通過 / ❌ 待修正 / ⚠️ 錯誤 統計橫幅 | v1.8 |
| 結果逐項展開 | 每個項次獨立可展開的結果卡片 | v1.8 |
| Chip 狀態標示 | 審查後 Chip 自動變色（綠通過、紅待修、黃錯誤）| v1.8 |
| API 請求間隔 2 秒 | 避免觸發 Gemini API Rate Limit | v1.8 |
| 下載 Markdown 報告 | 整份報告（總覽表格 + 逐項詳細結果）一鍵下載 | v1.8 |

### ✅ 免 API 金鑰 UX 引導（v1.9 新增 ⭐）
| 項目 | 說明 | 完成版本 |
|------|------|------|
| 無金鑰自動偵測 | 已上傳 PDF + 已選項次但未設 API Key，自動顯示引導面板 | v1.9 |
| 🔑 請先設定 API 金鑰徽章 | Step 3 按鈕旁顯示深色徽章，點擊捲動至引導面板 | v1.9 |
| 三步驟引導面板（動畫滑入） | 琥珀色漸層卡片，淡入+滑入動畫，視覺醒目不突兀 | v1.9 |
| Step 1：一鍵複製提示詞 | 含 Clipboard API + execCommand 雙重備援，複製後邊框變綠 | v1.9 |
| Step 2：開啟 Google Gemini | 若尚未複製，先自動複製再開新分頁；按鈕點後步驟標記完成 | v1.9 |
| Step 3：貼回 Gemini 回覆 | Textarea 接收回覆，套用後走相同 showResult() 渲染管線 | v1.9 |
| 套用後可複製 / 列印 / 下載報告 | 貼回結果與 API 審查結果完全等效，所有後處理均支援 | v1.9 |
| API Key 即時監聽（input event） | 輸入金鑰後引導面板即時消失，btnReview 立即啟用 | v1.9 |
| saveSettings 後同步更新 | 儲存設定後立即刷新按鈕與引導狀態 | v1.9 |
| 自動捲動至引導面板 | 引導面板首次出現時自動捲動到可視範圍 | v1.9 |
| 設定 Key 後重置步驟狀態 | 引導步驟的 nk-done 標記、已複製提示、貼回文字全部清除 | v1.9 |
| 列印時隱藏引導面板 | @media print 加入 .nokey-guide、#noKeyHint，列印不印出引導 | v1.9 |

### ✅ UI / 視覺設計
| 項目 | 說明 | 完成版本 |
|------|------|------|
| 全版 Hero 頁首 | 全寬漸層背景 + 三個裝飾光球 + 功能 Chip 標籤 | v1.6 |
| 專業 Footer（三欄格線） | 品牌說明 / 快速連結（含 🐛 回報問題）/ 技術棧徽章 | v1.7/v1.8 |
| Footer 彩色技術棧徽章 | Gemini AI（紫）、PDF.js v4（藍）、GitHub Pages（綠）、純前端（橘）、零後端（青） | v1.7 |
| Footer RWD | 768px → 兩欄；480px → 單欄堆疊 | v1.7 |
| 字型大小調整 | A⁻ / A / A⁺ 固定浮動控制器，localStorage 記住偏好 | v1.8 |
| PWA 安裝橫幅 | beforeinstallprompt 觸發，Header 顯示「加入主畫面」 | v1.8 |

### ✅ 社群分享與品牌識別
| 項目 | 說明 | 完成版本 |
|------|------|------|
| favicon.png / .ico | 多尺寸圖示，瀏覽器分頁顯示 | v1.3 |
| og-image.png（1200×630） | LINE / Facebook 分享預覽卡片大圖 | v1.3 |
| Open Graph meta tags | title / description / image / url 完整設定 | v1.3 |
| Twitter Card meta | summary_large_image 格式 | v1.3 |
| SEO description meta | 搜尋引擎描述文字 | v1.3 |

---

## 二、未來優化與開發建議

> 依難度與效益分為三個層級：**🟢 容易做（數小時～1–2 天）**、**🟡 中等（1 週內）**、**🔴 進階（需較多時間）**

---

### 🟢 容易做（立竿見影，建議下一波優先處理）

#### A. 🤖 無金鑰引導支援更多 AI 平台（v1.9 延伸）
**問題**：v1.9 引導僅連結 Google Gemini 網頁版，但有些老師已習慣使用 ChatGPT 或 Bing Copilot  
**建議**：
- Step 2 改為三個平台選擇按鈕：`🌐 Google Gemini`、`💬 ChatGPT`、`🔷 Microsoft Copilot`
- 依據使用者上次的選擇記憶（localStorage），下次自動選中同一平台
- 每個平台顯示小提示：Gemini「免費、無需登入」、ChatGPT「需帳號、免費版可用」
- 按鈕分組設計，三按鈕並排在 Step 2 卡片內
- **工作量估計**：30–60 分鐘（只需加兩個按鈕 + URL + localStorage）

#### B. 🔁 批次審查「重審失敗項目」
**問題**：批次審查 40 項後，只有少數項目失敗（API 錯誤或 Token 超限），不需要重跑全部  
**建議**：
- 在批次完成後的摘要列下方加「🔁 重審失敗項目」按鈕
- 只重新審查狀態為 `error` 的項目
- 同時加「❌ 重審待修正項目」（讓老師再確認紅字項目是否真的有問題）
- 重審完成後，更新原有的 Chip 狀態與結果卡片（不新增，直接覆蓋）
- **工作量估計**：1 小時（在現有 `batchResults` 基礎上過濾即可）

#### C. 📥 匯出 HTML 格式報告
**問題**：目前只能下載 Markdown，非技術老師不知道如何開啟  
**建議**：
- 新增「下載 HTML 報告」按鈕（放在「下載 Markdown 報告」旁）
- HTML 報告內建工具的 CSS 樣式（綠色通過卡片、紅色待修卡片，保留所有顏色）
- 可直接用瀏覽器開啟，也可列印轉 PDF（完美保留格式）
- HTML 報告標頭含工具名稱、學校名稱（若已設定）、審查日期、審查人
- **工作量估計**：2–3 小時

#### D. ⏱️ 批次審查預估時間提示
**問題**：老師選了 20 項要批次審查，不知道大概要等多久  
**建議**：
- 「開始批次審查」按鈕旁顯示：「已選 20 項，預計約 6–10 分鐘」
- 動態計算：每項 2 秒間隔 + 約 15–30 秒 AI 處理 = N × 17–32 秒
- 進度條旁顯示已用時間與剩餘預估時間，動態校正
- 完成後顯示「本次審查共耗時 X 分 Y 秒」
- **工作量估計**：30–60 分鐘

#### E. 📋 批次審查結果「全部展開 / 全部收起」
**問題**：批次審查完 40 項，結果區有 40 個折疊卡片，想一眼看完很麻煩  
**建議**：
- 摘要橫幅旁加兩顆按鈕：「全部展開 ▾」/ 「全部收起 ▸」
- 額外加「只展開待修正 ❌」快速按鈕，一鍵聚焦需要修改的項目
- 展開/收起全部時加輕量動畫
- **工作量估計**：30 分鐘

#### F. 🔔 批次審查完成聲音 + 桌面通知
**問題**：老師開著工具去做其他事，不知道審查完了  
**建議**：
- 批次完成時播放一個短促的通知音（Web Audio API 產生，完全不需要音訊檔案）
- 同時使用 `Notification API` 發送瀏覽器桌面通知（需使用者一次性授權）
- 通知內容：「課程計畫審查完成 ✅ 通過 12 項 ❌ 待修正 5 項」
- 在 API 設定區塊加入「開啟完成通知」開關，預設開啟
- **工作量估計**：1–2 小時

#### G. 🔍 批次審查結果篩選 Tab
**問題**：批次審查完 40 項，想快速找到所有「待修正」項目  
**建議**：
- 批次結果區頂部加篩選 Tab：「全部（40）」/「✅ 通過（12）」/「❌ 待修正（25）」/「⚠️ 錯誤（3）」
- 點擊 Tab 只顯示對應狀態的結果卡片（其他隱藏，不刪除）
- Tab 上的數字即時更新
- 也可加文字搜尋（輸入「統整」→ 只顯示含該關鍵字的項次）
- **工作量估計**：1–2 小時

#### H. 📋 無金鑰引導：引導步驟可收合記憶（v1.9 延伸）
**問題**：對於已熟悉流程的老師，每次都展開整個三步驟引導面板有點佔空間  
**建議**：
- 引導面板右上角加「收起 ▸」按鈕，收合後只顯示一行摘要：「🚀 免金鑰模式：複製提示詞 → Gemini → 貼回結果」
- 收合/展開狀態存 localStorage，下次開啟記住偏好
- 首次看到引導時自動展開，之後（曾使用過）預設收合
- 在收合狀態下，按鈕仍可直接操作（Step 1：複製；Step 2：開啟 Gemini；Step 3：展開貼回）
- **工作量估計**：1–2 小時

---

### 🟡 中等難度（功能性升級）

#### I. 🏫 學校基本資料設定
**問題**：每次下載的報告都沒有學校名稱，顯得不專業；AI 也不知道是哪所學校  
**建議**：在 API 設定區塊新增：
- 「學校名稱」輸入框（如：○○ 國小），存 localStorage
- 「學年度」下拉（115 / 116...），預設 115
- 「負責人 / 教務主任」欄位（選填）
- 報告標頭自動帶入：學校名稱、學年度、下載時間、審查人
- 審查 Prompt 動態帶入：「本校為桃園市○○國小，請依以下標準審查...」
- **效益**：AI 回應更具個人化（知道學校背景後，判斷更準確）
- **工作量估計**：2–3 小時

#### J. 📊 審查進度追蹤 Dashboard
**問題**：老師審了 15 個項次，不記得哪些通過、哪些還沒審  
**建議**：localStorage 記錄每次批次審查的最終狀態：
```
tyc_progress: {
  "0-1":   { status: "pass",   date: "2026-04-10", model: "gemini-2.5-flash" },
  "1-1":   { status: "revise", date: "2026-04-09", note: "缺少教師人數表" },
  "6-2-1-1": { status: "pending" }
}
```
- 批次審查完成後自動更新進度記錄
- 批次模式中，Chip 顯示歷史狀態顏色（已通過項次自動標綠）
- 頂部加「進度概覽」橫幅：「已確認通過 12 / 40 項（30%）🟩🟩🟩⬜⬜」
- 可手動覆寫狀態 + 加備註（「等校長簽名」、「已修正 v3」）
- 提供「清除全部進度」按鈕（帶確認對話框）
- **工作量估計**：半天

#### K. 📜 審查歷史記錄（最近 20 次單項審查）
**問題**：老師好不容易審完一份，下次開啟工具結果就不見了  
**建議**：localStorage 儲存最近 20 次單項審查記錄：
- 可展開的「歷史記錄」區塊：時間 / 項次 / 結果摘要（前 80 字）
- 點擊可重新查看完整結果（渲染在結果區）
- 點擊「再次審查」可重新組裝相同 Prompt 送出（舊 PDF 需重新上傳）
- 超過 20 筆自動刪除最舊的
- 提供「清除歷史」按鈕
- **延伸**：無金鑰模式貼回的結果也納入歷史記錄（v1.9 貼回的結果若已有 selectedItem，可記錄）
- **工作量估計**：3–4 小時

#### L. 🔗 分享特定項次連結
**問題**：教務主任要把某個項次的審查任務分配給老師，必須口頭說「你去開第二類，找 6-2-1-3」  
**建議**：
- URL 加入 Hash 參數，如：`/curriculum/#item=6-2-1-3`
- 分享連結後，接收者開啟頁面會自動預選到該分類與項次
- 在項次選單旁加「🔗 複製此項次連結」按鈕，點擊後 URL 更新並複製到剪貼簿
- 主任可直接傳連結分配工作，老師打開就直接到對應項次
- **工作量估計**：2–3 小時

#### M. ✏️ 提示詞模板管理（我的版本）
**問題**：每次審查「3 門課版本」都要手動改提示詞，麻煩  
**建議**：
- 允許儲存最多 5 個「我的提示詞模板」（存 localStorage）
- 模板可命名（如「3門課版本」、「合班版本」、「藝才班版本」）
- 下拉選單一鍵套用，當前提示詞區域即時更新
- 顯示「目前與預設版本的差異」提示（提醒使用者已修改）
- 提供「另存為我的模板」和「刪除模板」操作
- **工作量估計**：半天

#### N. 📱 批次審查結果一鍵分享（LINE / Email）
**問題**：審查完後，老師想把摘要傳給主任確認，目前只能截圖或複製貼上  
**建議**：
- 批次「下載報告」按鈕旁加「分享摘要」按鈕
- 點「分享到 LINE」→ 開啟 `line://msg/text/...`（LINE 分享 URL Scheme）
- 點「寄 Email」→ 開啟 `mailto:?subject=...&body=...` 帶入摘要文字
- 摘要格式：
  ```
  【課程計畫 AI 審查報告】桃園市 ○○ 國小 115 學年度
  審查時間：2026-04-10 14:30
  ✅ 通過 12 項 ❌ 待修正 25 項 ⚠️ 錯誤 3 項
  詳細報告請見附件或以下連結
  ```
- **工作量估計**：2–3 小時

#### O. 🌙 深色模式（Dark Mode）
**問題**：老師習慣夜間備課，白底畫面刺眼，長時間使用眼睛疲勞  
**建議**：
- 讀取系統偏好 `prefers-color-scheme: dark` 自動套用
- 字型大小控制器旁加入手動切換按鈕 🌞 / 🌙
- Dark Mode 色票：`--bg: #0f1a17`、`--card: #1a2e2a`、`--text: #e8f4f2`
- 狀態存 localStorage，重開視窗記住選擇
- 批次審查 Chip、進度條、結果卡片、免金鑰引導面板全部適配深色
- **工作量估計**：3–4 小時（CSS Custom Properties 架構讓切換非常容易）

#### P. 🌐 多主題切換（進階版）
**問題**：希望更個人化，不只有深色 / 淺色  
**建議**：提供多主題（只需修改 CSS Custom Properties，技術成本極低）：
- 🌿 **翠綠（預設）**：目前的 #1a8a7a 配色
- 🌙 **深夜護眼**：深色背景，低藍光配色
- 🎓 **學術藍**：藍色系，正式感強
- 🌸 **輕粉紫**：柔和的粉紫配色，適合女性使用者
- 設定區加「主題選擇」色票方塊，點擊即時切換
- **工作量估計**：1 天

---

### 🔴 進階功能（長期目標，需較多時間）

#### Q. 🤖 多 AI 模型同時對比審查
**概念**：同一份 PDF + 同一個項次，同時送給 Gemini 2.5-flash 和 Gemini 2.5-pro，左右並排顯示兩份結果  
**技術**：`Promise.allSettled()` 並行呼叫，介面左右兩欄  
**延伸**：可加入「AI 共識模式」——兩個模型都說「表件通過」才顯示大綠旗  
**效益**：降低 AI 幻覺（hallucination）影響，提高審查準確度  
**工作量估計**：半天 ~ 1 天

#### R. 📦 無金鑰批次模式（v1.9 進階延伸）
**概念**：目前 v1.9 的免金鑰引導只適用單項審查；老師想審 40 項但沒有 API Key  
**建議設計**：
```
批次提示詞打包下載：
  ①「打包下載所有提示詞」→ 產生一份含所有選中項次提示詞的 TXT 檔
     （每個項次有分隔符，方便逐一貼上）
  ② 老師逐一在 Gemini 審查，複製每份結果
  ③ 批次結果匯入介面：逐一貼回，工具自動對應到對應項次
```
- 另一種方案：「批次提示詞連結清單」——每個提示詞產生一個 `?prefill=...` 的 Gemini 短連結，老師逐一點開（但 Gemini 目前不支援 prefill）
- 更實際方案：分頁式引導介面，每次引導一個項次，完成後自動切換到下一項
- **工作量估計**：1–2 天（分頁式引導方案）

#### S. 📸 掃描版 PDF 支援（OCR 光學文字辨識）
**問題**：許多學校的課程計畫是掃描版（圖片 PDF），目前完全無法擷取文字，是最常見的使用障礙  
**方案一（免費）**：整合 **Tesseract.js**（開源前端 OCR）
- 全程在瀏覽器執行，不需後端
- 需下載中文語言包（約 30MB），載入時間較長
- 正確率依原稿清晰度而異

**方案二（付費，推薦）**：整合 **Google Cloud Vision API**
- 呼叫 Google OCR，中文準確率極高
- 需要 GCP 帳號與 Vision API 金鑰
- 免費額度：每月前 1,000 頁，教育用途通常夠用

**建議實作**：先做 Tesseract.js 方案，偵測到掃描版時自動提示「是否嘗試 OCR 辨識（需等待約 30 秒）？」  
**工作量估計**：1–2 天

#### T. 🗂️ 雙 PDF 比對（新舊版本差異分析）
**概念**：上傳「去年的課程計畫」與「今年草稿」，AI 找出哪些段落可能沒有更新  
**適用情境**：學校連年使用類似計畫，快速掃描遺漏更新的欄位  
**Prompt 設計**：
```
以下是【去年版本】課程計畫（節錄）：
---
{last_year_text}
---
以下是【今年草稿】課程計畫（節錄）：
---
{this_year_text}
---
請找出今年草稿中，可能遺漏更新的段落（與去年版本高度相似或完全相同），
並逐一列出「段落位置」、「去年內容」、「今年內容」、「修改建議」。
```
**技術**：同時上傳兩份 PDF，分別擷取文字，組裝比對 Prompt  
**工作量估計**：半天（UI 增加雙上傳區）

#### U. ☁️ Cloudflare Workers API Proxy（根本解決 CORS）
**問題**：OpenAI API 在 `file://` 環境下因 CORS 無法使用；未來若要支援更多 AI 供應商也有相同問題  
**方案**：部署輕量後端 Proxy：
```
前端 → https://proxy.xxx.workers.dev/gemini → Google Gemini API
前端 → https://proxy.xxx.workers.dev/openai → OpenAI API
前端 → https://proxy.xxx.workers.dev/claude → Anthropic Claude API
```
- **Cloudflare Workers 免費版**：每天 100,000 次請求，教育用途完全足夠
- 部署簡單：一個 JS 檔案部署到 Cloudflare，10 分鐘搞定
- 可在 Proxy 加入 Rate Limiting（防止他人濫用）
- 解鎖 Anthropic Claude、Mistral 等更多模型
- **工作量估計**：半天（含 Cloudflare 設定）

#### V. 🌏 多縣市 / 跨學年度版本
**概念**：目前 Prompt 資料是「桃園市 115 學年度」專用，其他縣市老師無法使用  
**技術架構**：設計可抽換的「審查標準資料包」：
```
criteria/
  taoyuan-115.json   ← 目前已有（內嵌於 HTML）
  taoyuan-116.json   ← 下一學年更新
  taipei-115.json    ← 臺北市版本
  keelung-115.json   ← 基隆市版本
```
- 工具首頁加入「縣市 / 學年度」選擇器，或支援匯入自訂 JSON
- 開放 GitHub PR，讓各縣市老師共同維護審查資料包
- 打造全台最完整的課程計畫 AI 審查資料庫
- **工作量估計**：先做資料分離（1 天），再做 UI 選擇器（1 天）

#### W. 🏆 審查品質評分（Scoring）
**概念**：目前 AI 只給「通過」或「待修正」，老師不知道嚴重程度  
**建議**：在 Prompt 中要求 AI 給出結構化評分：
```json
{
  "status": "revise",
  "score": 65,
  "critical": ["缺少課發會決議日期"],
  "minor": ["依據格式可更完整"],
  "suggestions": ["建議補充 (4) 項決議時間"]
}
```
- 介面顯示「符合度 65%」進度條（綠色 / 橘色 / 紅色漸變）
- Critical（必修）標紅色，Minor（建議修正）標橘色
- 批次審查後可按照「符合度分數」由低到高排序，優先處理最嚴重的
- **工作量估計**：1–2 天（Prompt 設計 + 結果解析）

#### X. 📊 匿名使用統計後台
**概念**：蒐集工具使用的匿名統計（不含任何個人資料或 PDF 內容）  
**統計內容**：
- 哪些項次最常被審查？
- 哪些項次的通過率最低（反映老師最易出錯的地方）？
- 哪個時段使用率最高（備課熱點）？
- 使用「免金鑰引導模式」vs「API 模式」的比例？（v1.9 新增追蹤點）

**技術**：Supabase 免費方案（500MB 資料庫），設計完全匿名 Schema：
```sql
CREATE TABLE reviews (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  item_id TEXT NOT NULL,          -- "6-2-1-1"
  result TEXT NOT NULL,           -- "pass" / "revise" / "error"
  model TEXT,                     -- "gemini-2.5-flash" / "no-key-manual"
  city_code TEXT,                 -- "taoyuan"
  mode TEXT,                      -- "api" / "nokey"（v1.9 新欄位）
  created_at TIMESTAMPTZ DEFAULT now()
);
-- 完全不儲存：PDF 內容、API 金鑰、學校名稱、IP、任何個資
```
- 資料公開供教育研究者使用
- 教育局可用來了解各校普遍性問題，作為輔導方向參考
- **工作量估計**：1–2 天（Supabase 設定 + API 整合）

#### Y. 🤝 Google 帳號登入 + 多裝置同步
**概念**：目前進度、提示詞模板都存 localStorage（只限一個瀏覽器）；換電腦就全部消失  
**技術**：整合 Google Identity API（OAuth 2.0）：
- 用 Google 帳號登入，不需要記帳號密碼
- 進度記錄 / 自訂模板 / 審查歷史同步到 Google Drive（JSON 格式）
- 多台電腦 / 手機同步，不喪失記錄
- **工作量估計**：2–3 天（OAuth 整合複雜，但 Google Drive API 有詳細文件）

#### Z. 🏫 學校主任管理面板
**概念**：全校多位老師各自審查不同項次，主任需要一個總覽頁面  
**技術**：需要後端（Supabase）+ Google 帳號登入：
- 老師上傳審查結果到雲端
- 主任登入後看到全校所有項次的完成狀況（誰負責哪個項次、狀態如何）
- 可指派任務給特定老師，可在項次上加入評論
- 匯出全校審查報告（含所有老師的審查結果彙整）
- **注意**：這已是 SaaS 等級的產品，需要較完整的後端架構
- **工作量估計**：1–2 週

---

## 三、技術負債與需要注意的問題

| 問題 | 影響程度 | 目前狀態 | 建議處理方式 |
|------|----------|------|------|
| Prompt 長度未限制 | 🔴 高 | 有字數警告但未截斷 | 加入字數截斷邏輯（保留前 N 字 + 尾部 + 提示） |
| API 金鑰明文存 localStorage | 🟡 中 | 已有「金鑰不上傳」說明 | 加入 session 過期機制，提示定期清除 |
| renderMarkdown() 自製解析器 | 🟡 中 | 對複雜 Markdown 可能渲染錯誤 | 考慮引入 marked.js（僅 20KB，輕量） |
| 批次審查無法跨頁儲存 | 🟡 中 | 關閉視窗即喪失批次結果 | 把 batchResults 存 sessionStorage，重開可恢復 |
| 單一 HTML 檔案持續增大（101KB）| 🟡 中 | 已超過合理上限，仍可正常使用 | 考慮拆分審查資料為獨立 `criteria.json`（fetch 載入） |
| OpenAI CORS 問題（file://） | 🟡 中 | 已有警告提示 | 完整解法需 API Proxy（Cloudflare Workers） |
| 無金鑰貼回結果無法驗證來源 | 🟢 低 | 老師自行把關 Gemini 回覆品質 | 提示「請確認來源為正確的 AI 回覆，非任意文字」 |
| Service Worker 快取更新機制 | 🟢 低 | 更新版本需手動改 CACHE_NAME | 加入版本自動更新邏輯 |
| 無自動化測試 | 🟢 低 | 全靠手動測試 | 加入基本 Playwright E2E 測試 |

---

## 四、優先順序建議

```
✅ 已完成（v1.9）：
  快捷鍵、列印、搜尋、字型調整、純文字複製、回報問題
  ⭐ 批次審查（全選/分類選/逐項/進度條/暫停繼續/摘要）
  ⭐ 報告下載（Markdown 整份報告）
  PWA（manifest.json + sw.js + 安裝橫幅）
  ⭐ 免 API 金鑰三步驟引導（自動偵測 → 複製 → Gemini → 貼回）

🔥 第二波（建議近期，1–2 週）：
  A. 多平台快捷按鈕      ← 30 分鐘，引導支援 ChatGPT / Copilot
  B. 重審失敗項目        ← 1 小時，批次使用者最需要
  E. 批次展開/收起全部   ← 30 分鐘，操作感大提升
  G. 批次結果篩選 Tab    ← 1–2 小時，快速找到待修正項目
  H. 引導步驟可收合      ← 1–2 小時，熟悉流程的老師不受干擾

⚡ 第三波（中期，1 個月）：
  C. 匯出 HTML 報告      ← 非技術老師更容易開啟和印出
  I. 學校名稱設定        ← 報告更專業，Prompt 更精準
  J. 審查進度 Dashboard  ← 40 項追蹤，知道還差幾項
  K. 審查歷史記錄        ← 防止辛苦審查結果消失
  D. 批次預估時間        ← 知道大概要等多久
  F. 完成聲音通知        ← 批次審查中可去做其他事

🚀 第四波（長期，3 個月）：
  L. 分享項次連結        ← 主任分配任務神器
  M. 提示詞模板管理      ← 常見調整版本一鍵套用
  N. LINE/Email 分享     ← 直送主任確認
  O. 深色模式            ← 夜間備課友善
  Q. 多 AI 對比審查      ← 提升審查準確度
  R. 無金鑰批次模式      ← 讓沒有 API Key 的老師也能批次審查
  S. 掃描版 OCR          ← 解決最大使用障礙
  T. 雙 PDF 比對         ← 新舊版本差異分析
  W. 審查品質評分        ← 量化符合度，更直觀

🌟 長期願景（6 個月以上）：
  U. Cloudflare Proxy    ← 解鎖所有 AI 供應商、根除 CORS
  V. 多縣市版本          ← 讓全台灣老師都能用
  X. 匿名統計後台        ← 教育大數據、輔助教育局政策
  Y. Google 帳號同步     ← 多裝置進度同步
  Z. 學校主任管理面板    ← SaaS 等級，讓全校共用
```

---

## 五、版本紀錄

| 版本 | 日期 | 主要更新內容 |
|------|------|----------|
| v1.0 | 2026-04 初 | 初始版本，40+ 審查項次，Gemini / OpenAI 雙 API，三步驟流程 |
| v1.2 | 2026-04-08 | 可編輯提示詞、AI 警語橫幅、重新上傳按鈕、特殊班級提醒、申請 API 金鑰按鈕 |
| v1.3 | 2026-04-10 | favicon（多尺寸）、OG 社群分享圖（1200×630）、Open Graph / Twitter Card / SEO meta |
| v1.4 | 2026-04-10 | 測試 API 金鑰（即時驗證 + 依錯誤類型的中文智慧提示） |
| v1.5 | 2026-04-10 | 結果美化（色彩標示）、金鑰顯隱、上次審查記錄、PDF 字數警告、PDF.js v4、項次白話說明 |
| v1.6 | 2026-04-10 | 全版 UI 升級：Hero 頁首、CSS 設計系統、三步驟彩色卡片、Google Fonts、RWD |
| v1.7 | 2026-04-10 | Footer 全面升級：深漸層三欄格線、彩色技術棧徽章、快速連結、隱私聲明、RWD |
| v1.8 | 2026-04-10 | ⭐ 九項功能大升級：快捷鍵、列印、搜尋、字型調整、純文字複製、回報問題、批次審查（Chip選/進度/暫停/摘要）、報告下載、PWA 安裝 |
| **v1.9** | **2026-04-10** | **⭐ 免 API 金鑰三步驟引導 UX：自動偵測無金鑰、複製提示詞、開啟 Gemini、貼回結果渲染；API Key 即時監聽；步驟視覺標記；列印隱藏引導** |

---

## 六、技術架構總覽（v1.9）

```
課程計畫 AI 審查工具
├── index.html            ← 101KB 單一檔案，純前端
│   ├── <head>
│   │   ├── PWA manifest / theme-color / apple meta
│   │   ├── Open Graph / Twitter Card / SEO meta
│   │   ├── favicon / apple-touch-icon
│   │   └── Google Fonts（Nunito + Noto Sans TC）
│   │
│   ├── <style>（內聯 CSS，~330 行）
│   │   ├── CSS Custom Properties（設計系統）
│   │   ├── Header / Hero / Header-chip / PWA banner
│   │   ├── Card（.card-step1/2/3）+ 字型大小控制
│   │   ├── 搜尋框 / 模式切換 Tab / 批次 Chip
│   │   ├── 批次進度條 / 結果卡片 / 摘要橫幅
│   │   ├── 免金鑰引導（.nokey-guide / .nokey-step）← v1.9 新增
│   │   ├── Footer（三欄 Grid + 技術棧徽章）
│   │   ├── @media print（A4 列印樣式）
│   │   └── @media（768px / 480px RWD）
│   │
│   ├── <body>
│   │   ├── <header>（全寬）Hero + PWA 安裝橫幅
│   │   ├── <div.font-size-ctrl>（固定浮動：A⁻/A/A⁺）
│   │   ├── <div.container>
│   │   │   ├── #lastReviewBar（上次審查記錄）
│   │   │   ├── <details#settingsPanel>（API 設定）
│   │   │   ├── .card.card-step1（PDF 上傳）
│   │   │   ├── .card.card-step2（選擇 + 批次審查）
│   │   │   │   ├── .mode-toggle（單項/批次 Tab）
│   │   │   │   ├── #singlePanel（搜尋框 + 分類選單 + 提示詞）
│   │   │   │   └── #batchPanel（Chip 選擇 + 進度 + 結果）
│   │   │   └── .card.card-step3（執行審查 + 結果 + 下載）
│   │   │       ├── .btn-group（送出 + 複製 + #noKeyHint 徽章）← v1.9 新增
│   │   │       ├── #noKeyGuide（三步驟引導面板）         ← v1.9 新增
│   │   │       │   ├── #nkStep1（複製提示詞）
│   │   │       │   ├── #nkStep2（開啟 Gemini）
│   │   │       │   └── #nkStep3（貼回結果 textarea）
│   │   │       ├── #loading（AI 審查中動畫）
│   │   │       └── #resultArea（結果 + 操作按鈕）
│   │   └── <footer>（全寬，深漸層三欄）
│   │
│   └── <script>（內聯 JS，~700 行）
│       ├── REVIEW_DATA[]（40+ 審查項次）
│       ├── ITEM_DESC{}（項次白話說明）
│       ├── loadPdfJs()（PDF.js v4 動態 import）
│       ├── filterItems() / onSearchSelect()（搜尋）
│       ├── setFontSize() / loadFontSize()（字型）
│       ├── copyPlainText()（純文字複製）
│       ├── testApiKey()（金鑰即時驗證）
│       ├── updateButtons()（含 API Key 偵測 + 引導顯隱）← v1.9 強化
│       ├── executeReview()（單項審查）
│       ├── noKeyCopyPrompt()（無金鑰：複製提示詞）      ← v1.9 新增
│       ├── noKeyOpenGemini()（無金鑰：開啟 Gemini）     ← v1.9 新增
│       ├── noKeyApplyResult()（無金鑰：套用貼回結果）   ← v1.9 新增
│       ├── noKeyScrollGuide()（無金鑰：捲動至引導）     ← v1.9 新增
│       ├── switchMode() / buildBatchPanel()（批次）
│       ├── startBatchReview() / runBatchQueue()（批次執行）
│       ├── togglePauseBatch() / showBatchSummary()（批次控制）
│       ├── downloadReport()（報告下載）
│       ├── renderMarkdown()（結果美化）
│       ├── installPwa() / SW 註冊（PWA）
│       ├── apiKey input 事件監聽（即時更新引導）        ← v1.9 新增
│       └── 快捷鍵監聽（Ctrl+Enter）
│
├── manifest.json         ← PWA 設定（名稱/圖示/色彩/截圖）
├── sw.js                 ← Service Worker（離線快取，cache v1.8）
├── favicon.png           ← 256×256 工具圖示
├── favicon.ico           ← 多尺寸 ICO（16/32/48/64/128）
├── og-image.png          ← 1200×630 社群分享大圖
├── generate_assets.py    ← Pillow 素材產生腳本
├── README.md             ← 使用說明文件
└── ROADMAP.md            ← 本文件（開發進度與規劃）
```

---

*課程計畫 AI 審查工具　｜　阿凱老師製作　｜　審查標準依據桃園市教育局115學年度*
